anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: anaconda/anaconda-auth
      use_max_tag: true
      include_regex: ^\d+\.\d+\.\d+$
    packages:
      - anaconda-auth
      - anaconda-cloud-auth
